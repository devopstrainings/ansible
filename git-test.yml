- name: Clone a repo 
  hosts: localhost 
  become: yes 
  tasks:
    - name: Create a directory 
      file:
        path: /tmp/ansible 
        state: directory 

    - name: Clone repo 
      git:
        